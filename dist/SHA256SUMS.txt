SHA256 Checksums for SHADES v1.2

Note: These checksums need to be regenerated after each build.
Run the following to verify:

  PowerShell:  Get-FileHash <file> -Algorithm SHA256
  cmd:         certutil -hashfile <file> SHA256

Checksums will be published with each GitHub Release.
